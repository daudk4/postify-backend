const userModel = require("./user");
const postModel = require("./post");
const pendingUserModel = require("./pendingUser");
const sessionModel = require("./session");

module.exports = {
  userModel,
  postModel,
  pendingUserModel,
  sessionModel,
};
